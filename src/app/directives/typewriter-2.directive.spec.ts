import { Typewriter2Directive } from './typewriter-2.directive';

describe('Typewriter2Directive', () => {
  it('should create an instance', () => {
    const directive = new Typewriter2Directive();
    expect(directive).toBeTruthy();
  });
});
