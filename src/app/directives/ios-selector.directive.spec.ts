import { IosSelectorDirective } from './ios-selector.directive';

describe('IosSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new IosSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
