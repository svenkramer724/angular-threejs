import { TypewriterPipe } from './typewriter.pipe';

describe('TypewriterPipe', () => {
  it('create an instance', () => {
    const pipe = new TypewriterPipe();
    expect(pipe).toBeTruthy();
  });
});
